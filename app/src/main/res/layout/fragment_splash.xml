<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bg_page"
    tools:context=".ui.SplashFragment">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:gravity="center"
        android:orientation="vertical"
        android:paddingStart="24dp"
        android:paddingEnd="24dp">

        <fcu.app.i_ching.ui.HexagramView
            android:id="@id/splash_hexagram"
            android:layout_width="108dp"
            android:layout_height="wrap_content"
            android:layout_marginBottom="30dp"
            android:contentDescription="@string/brand_title" />

        <TextView
            android:id="@id/splash_title"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:fontFamily="@font/noto_serif_tc_family"
            android:gravity="center"
            android:text="@string/brand_title"
            android:textColor="@color/ic_ink"
            android:textSize="38sp"
            android:textStyle="bold" />

        <TextView
            android:id="@id/splash_subtitle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:gravity="center"
            android:text="@string/splash_tagline"
            android:textColor="@color/ic_text_muted"
            android:textSize="16sp" />
    </LinearLayout>

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom"
        android:layout_marginBottom="48dp"
        android:fontFamily="@font/noto_serif_tc_family"
        android:gravity="center"
        android:text="易經"
        android:textColor="@color/ic_text_subtle"
        android:textSize="18sp" />
</FrameLayout>
