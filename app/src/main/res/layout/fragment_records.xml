<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bg_page"
    android:orientation="vertical"
    tools:context=".ui.RecordsFragment">

    <include
        android:id="@id/top_bar"
        layout="@layout/include_top_bar" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:paddingStart="24dp"
        android:paddingTop="24dp"
        android:paddingEnd="24dp">

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:fontFamily="@font/noto_serif_cjk_tc_regular"
            android:text="占卜紀錄"
            android:textColor="@color/ic_ink"
            android:textSize="36sp" />

        <EditText
            android:id="@id/records_search_input"
            android:layout_width="match_parent"
            android:layout_height="54dp"
            android:layout_marginTop="18dp"
            android:layout_marginBottom="12dp"
            android:background="@drawable/bg_input_underline"
            android:gravity="top|start"
            android:hint="@string/records_search_hint"
            android:inputType="text"
            android:minLines="1"
            android:padding="8dp"
            android:textColor="@color/ic_ink"
            android:textColorHint="@color/ic_outline_strong"
            android:textSize="16sp"
            tools:text="工作" />

        <HorizontalScrollView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:scrollbars="none">

            <com.google.android.material.chip.ChipGroup
                android:id="@+id/records_method_chip_group"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                app:singleLine="true"
                app:singleSelection="true">

                <com.google.android.material.chip.Chip
                    android:id="@id/records_method_all_chip"
                    style="@style/IChingFilterChip"
                    android:checked="true"
                    android:text="全部占法" />

                <com.google.android.material.chip.Chip
                    android:id="@id/records_method_coins_chip"
                    style="@style/IChingFilterChip"
                    android:text="三枚銅錢" />

                <com.google.android.material.chip.Chip
                    android:id="@id/records_method_yarrow_chip"
                    style="@style/IChingFilterChip"
                    android:text="蓍草靈感模式" />

                <com.google.android.material.chip.Chip
                    android:id="@id/records_method_simple_chip"
                    style="@style/IChingFilterChip"
                    android:text="簡易占法" />
            </com.google.android.material.chip.ChipGroup>
        </HorizontalScrollView>

        <HorizontalScrollView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:scrollbars="none">

            <com.google.android.material.chip.ChipGroup
                android:id="@+id/records_change_chip_group"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                app:singleLine="true"
                app:singleSelection="true">

                <com.google.android.material.chip.Chip
                    android:id="@id/records_change_all_chip"
                    style="@style/IChingFilterChip"
                    android:checked="true"
                    android:text="全部" />

                <com.google.android.material.chip.Chip
                    android:id="@id/records_change_with_chip"
                    style="@style/IChingFilterChip"
                    android:text="有變爻" />

                <com.google.android.material.chip.Chip
                    android:id="@id/records_change_without_chip"
                    style="@style/IChingFilterChip"
                    android:text="無變爻" />
            </com.google.android.material.chip.ChipGroup>
        </HorizontalScrollView>
    </LinearLayout>

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:paddingStart="24dp"
        android:paddingTop="18dp"
        android:paddingEnd="24dp"
        android:paddingBottom="18dp">

        <TextView
            android:id="@id/records_no_results"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:gravity="center"
            android:text="@string/records_no_results"
            android:textColor="@color/ic_text_muted"
            android:textSize="16sp"
            android:visibility="gone" />

        <include
            android:id="@id/records_empty_state"
            layout="@layout/include_empty_state"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:visibility="gone" />

        <androidx.recyclerview.widget.RecyclerView
            android:id="@id/records_list"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:clipToPadding="false"
            android:paddingBottom="8dp"
            tools:itemCount="3"
            tools:listitem="@layout/item_record" />
    </FrameLayout>

    <include
        android:id="@id/bottom_nav"
        layout="@layout/include_bottom_nav" />
</LinearLayout>
